# Requirements para PythonAnywhere
# Basado en base.txt pero sin gunicorn (PythonAnywhere usa su propio servidor WSGI)
# y sin channels-redis (no disponible en plan gratuito)

# Django y core
Django>=4.2.0,<5.0
python-decouple>=3.8
Pillow>=10.0.0
django-extensions>=3.2.0
whitenoise>=6.5.0
django-widget-tweaks>=1.5.0

# Base de datos MySQL
mysqlclient>=2.2.0

# Seguridad y autenticación
django-otp>=1.2.0
qrcode[pil]>=7.4.0
segno>=1.6.0

# Utilidades
python-dateutil>=2.8.0
requests>=2.31.0

# Analytics
user-agents>=2.2.0

# Data Science & ML
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
textblob>=0.17.0

# WebSockets (sin Redis — usa InMemoryChannelLayer en PythonAnywhere gratuito)
channels>=4.0.0
daphne>=4.0.0

# Content Editing
django-summernote>=0.8.20.0
